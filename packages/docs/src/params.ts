export const PARAM_KEY = 'docsx'

export const defaultParams = {
  /**
   * Show navigation at right sidebar.
   *
   * @default true
   */
  enableNavigation: true,
}

export type DocsxParams = typeof defaultParams
