{
  "name": "docs-example",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "start-storybook -p 6006",
    "cleanup": "rm -rf public",
    "build": "npm run cleanup && build-storybook -c .storybook -o public"
  },
  "devDependencies": {
    "@babel/core": "^7.6.0",
    "@storybook-addons/docs": "^1.2.0",
    "@storybook/addons": "^5.1.11",
    "@storybook/react": "^5.1.11",
    "babel-loader": "^8.0.6"
  }
}
